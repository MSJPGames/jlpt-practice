# 中文話者向けセクション 運用メモ（2026-07-29）

作成理由：香港在住という利用者の状況を踏まえ、**繁體字・広東語圏の学習者**に向けた入口と解説を新設した記録。あわせて OGP・sitemap・Search Console を整備した。**真実の元は OneDrive 実ファイル。**

## 位置づけ（なぜ作ったか）
- 利用者は**香港在住**、半年に一度帰国。日本国内の自治体・企業に売り込む形は物理的に取れない。
- 一方、**繁體字圏（香港・台湾・マカオ）＋広東語**は日本語学習の需要が大きいのに手薄。既存の中国語コンテンツは簡体字が主流。
- とくに**広東語は入声（-p/-t/-k）が残っている**ため、日本語の音読み・促音を規則的に予測できる。**普通話話者には原理的にできない**説明ができる＝当サイト固有の強み。
- 方針：**日本語は消さず、繁體中文を主にして日本語トグルを付ける**（日本語学習サイトなので母語だけで完結させない）。

## 新設ページ（9本・すべて最上位）
| ファイル | 内容 | 親 |
|---|---|---|
| `chinese_start.html` | 入口「用中文底子學日文」。同形異義語18組が中心 | index |
| `chinese_katakana.html` | 片假名。香港の音訳語（巴士/朱古力）と対照 | chinese_start |
| `chinese_doushi.html` | 動詞活用。三グループ・て形・「ている」の罠 | chinese_start |
| `chinese_bunkei.html` | 各活用形の使い道。5形31用法 | chinese_doushi |
| `chinese_joshi.html` | 助詞。「的」≠「の」など | chinese_start |
| `chinese_keigo.html` | 敬語。ウチ・ソトが中心 | chinese_start |
| `onyomi_cantonese.html` | 廣東話で音読みを読む（解説） | `lang.html?code=yue` |
| `onyomi_game.html` | 音読みゲーム（韻尾／音読み／促音の3モード） | onyomi_cantonese |
| `cangjie.html` | 倉頡字根で日本の漢字を見る ★下の「要判断」参照 | `lang.html?code=yue` |

- 学習順の導線：入口 → 動詞 → 文型 → 助詞 → 敬語（各ページ末尾に次への大きなリンク）。
- `index.html` に5枚目の入口カード（🈶 用中文底子學日文）、`gaikokugo.html` の「読みもの・コラム」にも1枚追加。

## 共通の作り（新規ページを足すときはこれに合わせる）
- **言語トグル**：`body.lang-zh` / `body.lang-ja` を切り替え、`.zh` / `.ja` の表示を出し分け。`localStorage` にキー保存（`nnh_*_lang`）。`<html lang>` も連動。
- **繁體中文は書面語ではなく口語**（「一句講晒」「試吓」「你已經捉到個規律喇」）。書面語だと台湾のサイトと区別がつかず、香港の読者に「自分向け」と伝わらない。
- **ナビ**：`‹ 親名` ＋ `🏠 首頁/トップ`（ナビ標準化メモの方針どおり）。トグルバーの右端に置く。
- **練習**：各ページ末尾に8問前後のランダム出題。もう一度ボタンで再挑戦。
- **OGP**：全ページに `og:title/description/image` ＋ `twitter:card=summary_large_image` ＋ `canonical`。画像は 1200×630 の `og_*.png`（htmlと同じ階層）。テンプレは作業環境の `ogp_template.html` で生成。

## データの外部化と編集ページ（2ゲーム）
巻き戻り・修正のしやすさのため、ゲームは**データを別ファイルに切り出し**、専用の編集ページを付けた。
- `gomi_rush.html` ＋ `gomi_data.js` ＋ `gomi_edit.html`
- `onyomi_game.html` ＋ `onyomi_data.js` ＋ `onyomi_edit.html`
- 方式は BJT/J.TEST 系と同じ**「ブラウザで編集 → data.js を書き出し → GitHubに上げる」**（サーバ不要）。CSV（BOM付き）の出し入れも可能＝エクセルで一括修正できる。
- 編集ページは**自分専用**。`noindex,nofollow` 済み。トップからはリンクしない。
- **編集ページの主役は検証バー**。画面上部に「行き先 OK／訳語 OK」等を常時表示し、壊れた瞬間に赤い数字になる。指摘を受けて直すためのページなので、壊したことに気づける方を優先した。

## ゴミ分別ゲームの多言語（9言語）
- 英・简体中文・繁體中文・ベトナム語・韓国語・ポルトガル語・タガログ語・スペイン語・インドネシア語。
- **訳語は自治体の多言語ごみ案内の公的表記に合わせた**（尾張旭市の各言語対応表、西尾市の4言語カレンダー、那須烏山市、千葉市韓国語版、田原市インドネシア語版）。`TR.sources` に記録。
- 語（単語）は標準語彙として使用可。**説明文は当サイトのオリジナル**。用語集を丸ごと写すと編集著作物（著作権法12条）の複製になりうるので、複数自治体を横断して共通語を採る方式にした。
- **繁體中文は簡体字からの機械変換ではない**。語彙が違う：纸板→**紙箱**、塑料→**塑膠**、厨余垃圾→**廚餘**、荧光灯→**光管**、冰箱→**雪櫃**。
- 未整備の言語は英語にフォールバック（`tr()` 内 `e[LANG]||e.en`）。
- ネパール語・タイ語・ミャンマー語は**公的な用語対応表を入手できず見送り**。福生市（越・タガログ・ネパール）、豊島区（ミャンマー・ネパール・タイ・ヒンディー）、大阪市「ごみのマナーABC」（**CC-BY 4.0** で明示）が候補。

## langdata.js への登録
- 中国語系4言語（`yue` / `zhtw` / `nan` / `zh`）の `extras` に新ページを登録。yue は10件になった。
- 追加の書き方：`extras:[...]` の中に `{href,title,desc}` を足すだけ。**1行の変更で済む**ので、差分が1行であることを毎回確認する。

## 検索まわり（2026-07-29）
- **`sitemap.xml`（122件）**：サイトを実際に辿って収集＋`langdata.js` から23言語ぶんを生成。管理用（`edit_all` / `gomi_edit` / `onyomi_edit`）は除外。**ページを足したら追記が必要。**
- **Google Search Console**：URLプレフィックス `https://msjpgames.github.io/jlpt-practice/` で登録。
- ⚠ **`index.html` の `<head>` にある `<meta name="google-site-verification" content="AsWuV7hfxUBOIb2mOSGi7zNbryGNf8tILxyx6jlsxWk" />` は絶対に消さない。** 消すと所有権が外れ、データが見られなくなる。index.html を書き換えるときは毎回この1行の存在を確認する。
- **robots.txt は入れていない**。GitHub Pages ではドメイン直下（`msjpgames.github.io/robots.txt`）でないと読まれず、`jlpt-practice/` の中では無視されるため。sitemap は Search Console から直接送信できるので不要。管理ページの除外は `noindex` メタで行う。`edit_all.html` にも入れておくとよい。

## 制作中に踏んだ落とし穴（再発注意）★
1. **`../index.html` でフォルダ外へ脱出**（`onyomi_cantonese.html` で発生）。最上位ページで `../` を書くとサイトの外に出る。ナビ標準化メモにある `hatsuon.html` と同じ事故。**最上位からは必ず `index.html`。**
2. **日本語の例文に繁体字を使ってしまう**（`chinese_joshi.html` で発生：學生です／圖書館で 勉強します）。繁體中文で書いていると引きずられる。**例文の漢字は必ず新字体**（学生／図書館）。しかも一度は脚注で「繁体字のままにしています」と正当化まで書いていた＝より悪い。
3. **日本の新字体と中国の簡体字の取り違え**（`onyomi_data.js` で発生：東→**东**、電→**电**、動→**动** と書いていた）。日本では東・電・動のまま。**日本語教育サイトでは致命的な誤り。**
4. **zipのファイル名が文字化け**。`zip` コマンドはUTF-8フラグを立てないことがあり、Windowsで `πü»πüÿ…` になる。**zip内のファイル名はASCIIにする**（README.txt 等）。日本語は中身だけに。
5. **同梱テキストの文字化け**。BOM無し・LF改行だとWindowsのメモ帳で化ける／1行に潰れる。**UTF-8（BOM付き）＋CRLF**で書く。
6. **同じファイルが複数のzipに入って、古い版で上書きされる危険**。作業を重ねたら**最終版だけの統合zipを作り直す**（`ALL_FINAL.zip` として実施）。
7. **練習問題の完成文が壊れる**（`chinese_bunkei.html`：「待ってくださいです」）。空欄の後ろの文字列を消し忘れる。**完成文を全問書き出して目視**する。
8. **選択肢の重複**（`onyomi_game.html`：さん／しん／きゅう など同じ音読みの字が複数あるため 200回中9回発生）。**重複除去を入れる。**

## 音読みゲームの呉音・漢音対応（2026-07-29 修正）★
- **指摘**：「明」は めい も みょう も正しいのに、片方しか正解と見なされていなかった。→ バグ。データが1字につき読みを1つしか持っていなかった。
- **直した内容**
  - `onyomi_data.js` に `ons:[{r:読み, k:呉/漢/慣, w:例語}]` を追加。**35字**に複数読みを登録。`on` は代表読み（表の見出し用）として残す。
  - ゲーム側：音読みあてモードで**その字の正しい読みならどれでも正解**に。あわせて**誤答にその字の別の読みが混ざらない**ようにした（「生」の問題で「しょう」が誤答に出ると、正しいのに不正解になる）。解説は呉音・漢音を例語つきで両方表示。
  - 解説ページに「一個字，兩個音 — 吳音同漢音」の節を新設。
  - 編集ページ：読みの欄を `めい 漢 説明 せつめい / みょう 呉 明日 みょうにち` の形式で複数対応。CSV列名も「音読み」→「読み」。検証は複数の読みすべてを韻尾規則にかける（慣用音は対象外）。
- **副産物＝教える材料になった**：**-ng の字は呉音＝う（よう・おう）／漢音＝い（えい）で規則的に対立する。**明 みょう/めい、京 きょう/けい、生 しょう/せい、經 きょう/けい、兄 きょう/けい、成 じょう/せい、青 しょう/せい。両方正しいのは偶然ではなく規則。
- **教訓**：1項目1値のデータ設計は、言語データでは早すぎる決めうちになる。**呉音・漢音・慣用音のように複数が正しい領域では、最初から配列で持つ。**

## 検証方法（このセクションで使ったもの）
- Playwright（Chromium `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`）で `file://` を開き、**pageerror 0**・言語トグル・練習の全問通し・360/390/768/1100px で**横あふれ無し**を確認。
- データは node で機械検査：
  - ゴミ分別＝全品目×全都市で行き先が解決するか、訳語の欠けが無いか。
  - 音読み＝**韻尾と音読みが規則どおりか**（-k なら く/き で終わる 等）を全110字で照合。例外は `note` を持つものだけ許容。
  - 敬語＝**尊敬語の欄に謙譲語が混ざっていないか**（方向の取り違えはこのページで唯一あってはならない誤り）。
  - 動詞＝て形・ます形・ない形・た形が活用規則どおりか全19語で照合。
- zip は**解凍し直して**全ページを開き直す（同梱漏れ・パス切れの検出）。

## 要判断・未対応 ★
- ✅ **`cangjie.html` は 2026-07-29 版で確定**（利用者の判断）。2026-07-21 に同名ページがあり上書きしたが、**新版で運用する**。旧版は GitHub のコミット履歴に残っている。以後この件は判断済みとして扱う。
- ✅ **`gomi_rush.html` の「試作版」バッジは削除**（2026-07-29）。`.proto` の CSS も未使用になったので併せて削除。
- **ネパール語・タイ語・ミャンマー語**の追加（上記の資料が候補）。
- **共有用の文面**（香港のFacebookグループ・Threads・LIHKG、Reddit r/Cantonese 向け）は未作成。利用者の希望で後回し。
- **アクセス解析**：Search Console のみ。閲覧数まで見たい場合は Cloudflare Web Analytics（Cookie・フィンガープリント無し）を各ページに1行追加する。
- **`sitemap.xml` の更新**：ページを足したら追記する。作業環境の `mksitemap.py` に一覧がある。
